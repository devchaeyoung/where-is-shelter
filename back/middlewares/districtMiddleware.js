import shelters from "../db/schemas/shelter";

const shelterDistrictMiddleware = (req, res, next) => {
  //   const { district } = req.query;

  //   req.district = shelters[]
  //   next();
  return;
};

module.exports = shelterDistrictMiddleware;
